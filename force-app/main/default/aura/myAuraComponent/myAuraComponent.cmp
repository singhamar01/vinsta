<!-- myAuraComponent.cmp -->
<aura:component>
    <ltng:require scripts="{!$Resource.SharedUtils}" afterScriptsLoaded="{!c.handleScriptsLoaded}"/>
    <div>Check console for SharedUtils output</div>
</aura:component>