/**
 * Child class SURR_ChannelAPIUtility extending ChannelAPIBaseUtility.
 * This class does not override any methods, inheriting all behavior from the base class.
 * Demonstrates basic inheritance where the child uses the parent's implementation as-is.
 * Best practices:
 * - Extends the virtual base class.
 * - No overrides, showing default inheritance.
 */
public class SURR_ChannelAPIUtility extends ChannelAPIBaseUtility {
    // No overrides here - uses base class methods directly.
}